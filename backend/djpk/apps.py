from django.apps import AppConfig


class DjpkConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "djpk"
    verbose_name = "DJPK (Kemenkeu) regional finance / APBD data"
